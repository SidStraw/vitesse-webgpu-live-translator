# Components

Vue components that are shared across different extension contexts (popup, options, content script).

- `Logo.vue` - Extension logo component
- `SharedSubtitle.vue` - Shared subtitle display component
